# Copy this file to .env and fill in values
# URL of the FastAPI backend server
VITE_API_URL=http://localhost:8000

# Optional: Enables GPS reverse geocoding (coords → city name)
# Without this, GPS coords are shown as lat/lng numbers
# Enable "Geocoding API" at console.cloud.google.com
VITE_GOOGLE_MAPS_KEY=
